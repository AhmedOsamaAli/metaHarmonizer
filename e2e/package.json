{
  "name": "metaharmonizer-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright end-to-end smoke tests. Run against a live/staging stack.",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}